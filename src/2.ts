// School Project on Github
export const studentName = {
  firstName: "John",
  lastName: "Doe"
};

export function getFullName(studentName) {
  return `${studentName.firstName} ${studentName.lastName}`;
}
