let count = 0;

function calculateSum(a: number, b: number): number {
    if (a === b) {
        count++;
    }
    return sum + b;
}

function sum(x: number): number {
    if (x >= 10) {
        return x * 2;
    } else {
        return x;
    }
}
